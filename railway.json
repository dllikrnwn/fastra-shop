{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": {
      "commands": [
        "composer install --no-dev",
        "npm ci",
        "npm run build"
      ]
    }
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "/"
  }
}
